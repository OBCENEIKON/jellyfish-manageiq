require 'spec_helper'

describe Jellyfish::ManageIQ do
  it 'has a version number' do
    expect(Jellyfish::ManageIQ::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end